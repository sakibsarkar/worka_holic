'use client';
import { useSession } from 'next-auth/react';
import React from 'react';

const ProfilePage = () => {
    
    return (
        <div>
            Profile
        </div>
    );
};

export default ProfilePage;